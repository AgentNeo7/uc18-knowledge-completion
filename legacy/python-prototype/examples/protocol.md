Original synthetic fixture assertions frozen before implementation. Expected JSON is a recursive subset oracle, not a generated output or independent holdout. Three cases cover positive, negative and unknown. Algorithms receive only input data. Deterministic input contract v1 uses declared synthetic records; no native runtime, real people, credentials or production writes. Passing means these assumptions match, not full requirement acceptance or superiority.
